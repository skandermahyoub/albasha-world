import OrderTimeline from '@/components/orders/OrderTimeline';

export default function OrderDetailsCard({ order }) {
  return <div className="space-y-4"><section className="bg-card border border-border/50 rounded-2xl p-4"><h2 className="font-bold mb-3">المنتجات</h2>{order.items?.map((item, index) => <div key={`${item.product_id}-${index}`} className="flex justify-between gap-3 py-2 border-b border-border/40 last:border-0 text-sm"><span>{item.title} × {item.quantity}</span><span>{item.price * item.quantity} {order.currency || 'USD'}</span></div>)}<div className="pt-3 space-y-1 text-sm"><div className="flex justify-between"><span>المجموع الفرعي</span><span>{order.subtotal} {order.currency || 'USD'}</span></div>{order.discount > 0 && <div className="flex justify-between text-green-600"><span>الخصم</span><span>-{order.discount} {order.currency || 'USD'}</span></div>}<div className="flex justify-between font-bold text-primary pt-2"><span>الإجمالي</span><span>{order.total} {order.currency || 'USD'}</span></div></div></section><section className="bg-card border border-border/50 rounded-2xl p-4"><h2 className="font-bold mb-3">حالة الطلب</h2><OrderTimeline order={order} /></section><section className="bg-card border border-border/50 rounded-2xl p-4 text-sm space-y-2"><h2 className="font-bold">التسليم</h2>{order.address && <p>العنوان: {order.address}</p>}{order.shipping_zone && <p>منطقة التوصيل: {order.shipping_zone}</p>}{order.notes && <p>ملاحظات: {order.notes}</p>}{order.payment_method && <p>طريقة الدفع: {order.payment_method}</p>}</section></div>;
}